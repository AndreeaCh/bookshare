import A from "../../constants/actions";
import M from "../../constants/mutations";
import Vue from "vue";

const state = {
};

const actions = {
};

const mutations = {
};

const getters = {};

export default {
  state,
  actions,
  getters,
  mutations
};
